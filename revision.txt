Ticket Priority Predictor - Detailed Revision Document
======================================================

0) What This Document Covers
----------------------------
This document is a complete technical revision of the project with:
- Full stack summary.
- File/folder purpose mapping.
- Backend API and runtime behavior.
- Frontend architecture and state flow.
- ML integration points.
- Security, deployment, and operations notes.


1) System Purpose and Business Flow
-----------------------------------
This project is an IT support/ticketing platform where users can:
1. Create and manage support tickets.
2. Assign tickets to teams and members.
3. Track ticket status and analytics.
4. Use AI to predict priority and support decision making.

Core flow:
1. User creates ticket from frontend form.
2. Backend receives request and runs priority prediction.
3. Backend saves ticket in MongoDB.
4. Socket events notify connected clients.
5. UI updates dashboards, lists, and detail pages.


2) Top-Level Files and Their Roles
----------------------------------
eslint.config.js
- ESLint flat config for both frontend and backend paths.
- Defines lint behavior and warnings/errors policy.

index.html
- Vite entry HTML template where frontend bundle mounts.

package.json
- Root/frontend dependency and script manifest.
- Contains frontend scripts (dev, build, lint, preview).

postcss.config.js
- PostCSS pipeline config, used with Tailwind/autoprefixer.

tailwind.config.js
- Tailwind design token and content scanning config.

vite.config.js
- Frontend bundler config.
- Includes plugin setup and manual chunk splitting strategy.

README.md
- Project setup and usage notes (human-facing overview).

skills-lock.json
- Workspace/tooling lock for skills metadata.

ta
- Misc workspace file (not part of runtime path).

dist/

public/
- Static assets served by Vite.

src/
- Entire React frontend source.

backend/
- Entire Node.js API and ML inference service.

.agents/
- Agent skill definitions and scripts (development assistant tooling).


3) Frontend File-by-File Map (src)
----------------------------------
src/main.jsx
- React root bootstrap and application mount.

src/App.jsx
- Main app router/layout composition entry.

src/App.css
- App-level styles.

src/index.css
- Global styles, Tailwind layer imports, base theme classes.

src/assets/
- UI assets (icons/images if present).

src/components/common/AIAssistantWidget.jsx
- Embedded assistant widget UI and interaction shell.

src/components/common/Badge.jsx
- Reusable badge/chip component for statuses/tags.

src/components/common/Breadcrumbs.jsx
- Route breadcrumb navigation UI.

src/components/common/Button.jsx
- Reusable button variant system.

src/components/common/Card.jsx
- Standard card container abstraction.

src/components/common/CreateTeamModal.jsx
- Team creation modal form and submit flow.

src/components/common/EmptyState.jsx
- Empty-list/empty-page placeholder component.

src/components/common/Input.jsx
- Shared input control styling wrapper.

src/components/common/LoadingSkeleton.jsx
- Skeleton loading placeholders.

src/components/common/Modal.jsx
- Generic modal overlay and close behavior.

src/components/common/PageTransition.jsx
- Route/page animation wrapper.

src/components/common/Toast.jsx
- Notification view helper.

src/components/dashboard/ActivityTimeline.jsx
- Ticket/team activity timeline visuals.

src/components/dashboard/PriorityChart.jsx
- Priority distribution chart (Recharts based).

src/components/dashboard/QuickActions.jsx
- Dashboard quick action shortcuts.

src/components/dashboard/RecentTicketsTable.jsx
- Recent ticket listing block for dashboard.

src/components/dashboard/StatCard.jsx
- KPI summary card (counts, trends, labels).

src/components/dashboard/TrendingCarousel.jsx
- Trending insights carousel/rotator.

src/components/layout/Layout.jsx
- Global app shell composition (sidebar/topbar/content).

src/components/layout/Sidebar.jsx
- Main navigation sidebar.

src/components/layout/TeamSelector.jsx
- Team context selector.

src/components/layout/Topbar.jsx
- Header controls and top-level actions.

src/components/auth/ProtectedRoute.jsx
- Route guard for authenticated pages.

src/components/analytics/
- Analytics-specific reusable visualization components.

src/components/tickets/
- Ticket-centric reusable components (forms/items/details helpers).

src/contexts/
- React context containers (if used by current pages/components).

16) April 2026 Frontend Navigation and Section Expansion
--------------------------------------------------------
This update delivered implementation-plan modules and corrected production navigation behavior.

A) New Functional Pages Added (MVP)
1. Work queue and operational views:
  - src/pages/MyTickets.jsx
  - src/pages/UnassignedTickets.jsx
  - src/pages/UrgentTickets.jsx
2. Insights and reporting views:
  - src/pages/Reports.jsx
  - src/pages/Performance.jsx
  - src/pages/ActivityLog.jsx
3. Platform and support views:
  - src/pages/NotificationsCenter.jsx
  - src/pages/SavedViews.jsx
  - src/pages/Customers.jsx
  - src/pages/CalendarSchedule.jsx
  - src/pages/KnowledgeBase.jsx
  - src/pages/TemplatesPage.jsx
  - src/pages/Automations.jsx
  - src/pages/Integrations.jsx
  - src/pages/SLAManagement.jsx
  - src/pages/HelpSupport.jsx
  - src/pages/Inbox.jsx

B) Navigation and Layout Stabilization
1. Sidebar expanded with implementation-plan entries and role-aware visibility rules:
  - src/components/layout/Sidebar.jsx
2. Sidebar made reliably scrollable with full-height flex layout and min-height-safe nav region.
3. Breadcrumb label map expanded for newly added pages:
  - src/components/common/Breadcrumbs.jsx
4. Topbar profile dropdown Help action now routes to Help page:
  - src/components/layout/Topbar.jsx

C) Route Mapping Hardening
1. Added explicit route mappings and aliases for section names (for compatibility with old/deep links).
2. Added deterministic protected-page rendering pattern to avoid fallback collisions.
3. Included route aliases for common naming variants:
  - high-priority -> urgent
  - notifications-center -> notifications
  - sla-management -> sla
  - help-support -> help
  - create-ticket -> create

D) Supporting Utility Enhancements
1. Added calculateSLAStatus helper for SLA cohort rendering and saved-view filtering:
  - src/lib/utils.js
2. Improved team URL/store synchronization stability in team filter hook:
  - src/hooks/useTeamFilter.js

E) UX Connectivity Improvements
1. SLA Management now links directly to Saved Views cohorts (at-risk/breached).
2. Notifications and knowledge/support pages use in-app route links for SPA-safe navigation.
3. Module pages consume existing backend data and reuse realtime/React Query refresh patterns.

src/data/mockData.js
- Mock datasets used by analytics/dashboard/team views.

src/hooks/useTeamFilter.js
- Team-aware filtering and URL/state synchronization hook.

src/lib/utils.js
- Generic helper utilities (class merges/string helpers/etc).

src/pages/Analytics.jsx
- Analytics screen and chart composition.

src/pages/CreateTicket.jsx
- Ticket creation form, validation, submit + AI suggestion trigger.

src/pages/Dashboard.jsx
- Main dashboard with summary metrics and activity.

src/pages/ForgotPassword.jsx
- Forgot password request screen.

src/pages/Login.jsx
- Login form and auth flow.

src/pages/ResetPassword.jsx
- Password reset completion screen.

src/pages/Settings.jsx
- User and system preferences page.
- Contains AI model health card with refresh and polling.

src/pages/SignUp.jsx
- User registration screen.

src/pages/Team.jsx
- Team details, members, workloads, team ticket data.

src/pages/TicketDetail.jsx
- Single ticket details + comments + update actions.

src/pages/TicketList.jsx
- Search/filter/sort/pagination listing of tickets.

src/services/api.js
- Axios client setup.
- Token storage helpers.
- Service groups: authAPI, ticketsAPI, teamsAPI, usersAPI, uploadAPI, aiAPI.

src/store/useStore.js
- Zustand global store (theme, app state, cross-page toggles).


4) Backend File-by-File Map (backend)
-------------------------------------
backend/package.json
- Backend dependency manifest and scripts.
- Includes check:model utility script.

backend/server.js
- API server entrypoint.
- Loads env, connects DB, applies middleware.
- Mounts routes, health endpoint, error handler.
- Initializes Socket.IO server.

backend/config/cloudinary.js
- Cloudinary configuration for media upload integration.

backend/config/db.js
- MongoDB connection bootstrap using mongoose.connect(uri).

backend/controllers/authController.js
- Register/login/me/logout and password reset controller logic.

backend/controllers/teamController.js
- Team CRUD, team member operations.

backend/controllers/ticketController.js
- Ticket CRUD, comments, stats, similar tickets, CSV export.
- Integrates predictPriority before ticket creation.

backend/controllers/userController.js
- User profile and performance endpoints.

backend/middleware/auth.js
- JWT verification and request user extraction.

backend/middleware/errorHandler.js
- Unified API error response formatter.

backend/middleware/validation.js
- express-validator rules and validation handling.

backend/models/Comment.js
- Comment subdocument/schema model definition.

backend/models/Team.js
- Team schema, metadata, member relations.

backend/models/Ticket.js
- Ticket schema including status, priority, tags, AI predictions, comments.

backend/models/User.js
- User schema, auth fields, team/expertise metadata.

backend/models/priority_model.onnx
- Exported machine-learning model used for local priority inference.

backend/routes/ai.js
- AI endpoints:
  - POST /api/ai/chat
  - POST /api/ai/suggest-priority
  - GET /api/ai/model-health

backend/routes/auth.js
- Authentication route declarations and validators.

backend/routes/teams.js
- Team route declarations.

backend/routes/tickets.js
- Ticket route declarations.

backend/routes/upload.js
- Upload endpoint(s) and storage handling.

backend/routes/users.js
- User route declarations.

backend/scripts/seed.js
- Seed utility for initial database population.

backend/scripts/train.py
- Model training script (RandomForest -> ONNX export).

backend/scripts/checkModel.js
- Script to run one prediction and print result.

backend/scripts/checkModelHealth.js
- Script to print model runtime health metadata.

backend/scripts/ITSM/
- Reference research notebook and dataset notes used for ML experimentation.

backend/uploads/
- Local upload destination when cloud storage is not used.

backend/utils/helpers.js
- General backend helper methods (example: ticket ID generation).

backend/utils/mlPredict.js
- Core ML inference + fallback heuristic implementation.
- Exposes predictPriority and getModelHealth.


5) Exact Tech Stack and Why It Matters
--------------------------------------
This section explains each major technology in plain terms:
1. What it is.
2. Where you used it.
3. What impact it had on your project.

Backend Runtime and API Layer
1. Node.js
- What it is:
  JavaScript runtime outside the browser.
- Where used:
  Entire backend runtime in backend/server.js and all backend JS files.
- Impact on your project:
  Lets you run backend logic in the same language family as frontend (JS/React), reducing context switching and speeding development.

2. Express.js
- What it is:
  Lightweight web framework for Node.js.
- Where used:
  Route/middleware server composition in backend/server.js and backend/routes/*.
- Impact on your project:
  Gives a clear API structure (auth, tickets, teams, users, ai, upload) and makes middleware-based security and validation easy to apply consistently.

3. MongoDB
- What it is:
  NoSQL document database.
- Where used:
  Primary data store for users, teams, tickets, comments.
- Impact on your project:
  Flexible schema evolution for ticket fields, AI metadata, and nested document patterns without heavy relational migrations.

4. Mongoose
- What it is:
  ODM (Object Document Mapper) for MongoDB.
- Where used:
  backend/models/* and backend/config/db.js.
- Impact on your project:
  Adds schema definitions, validation hooks, and query convenience, which improves maintainability and data consistency.

5. JWT (jsonwebtoken)
- What it is:
  Token-based auth mechanism.
- Where used:
  backend/controllers/authController.js and backend/middleware/auth.js.
- Impact on your project:
  Enables stateless auth and protected endpoints like model-health, team updates, and ticket operations.

6. bcryptjs
- What it is:
  Password hashing library.
- Where used:
  Authentication flow in auth controller/model logic.
- Impact on your project:
  Prevents plain-text password storage and strengthens account security.

7. express-validator
- What it is:
  Request validation toolkit.
- Where used:
  backend/middleware/validation.js and route validators.
- Impact on your project:
  Blocks malformed payloads early and provides cleaner error handling.

8. multer + cloudinary + multer-storage-cloudinary
- What it is:
  File upload middleware + cloud media storage integration.
- Where used:
  backend/routes/upload.js and backend/config/cloudinary.js.
- Impact on your project:
  Supports ticket attachments and media handling with local/cloud options.

9. Socket.IO


15) April 2026 Consolidated Revision Log
----------------------------------------
This section captures all major implementation changes completed in the latest development cycle.

A) Data Quality, Seed and Realistic Ticket Corpus
1. Added large realistic ticket feed script:
  - backend/scripts/feedGenuineTickets.js
2. Imported 60 realistic tickets across categories:
  - Authentication, Notifications, Performance, Database, Integration,
    Attachments, Billing, Mobile, Authorization, Reporting.
3. Removed junk placeholder ticket data (hiegvegfef) with cleanup utility:
  - backend/scripts/cleanupJunkTickets.js
4. Added assignment utility to ensure tickets are visible under active team filters:
  - backend/scripts/assignTicketsToDefaultTeam.js

B) Similar Tickets and Resolver Enhancements
1. Fixed non-clickable similar tickets in create flow.
2. Enhanced similar-ticket backend payload with likely resolution text snippets
  from previously resolved/closed incidents.
3. Improved navigation safety when similar tickets lack an id.

C) Priority Model and AI Classification Strengthening
1. Upgraded heuristic classifier with weighted keyword rules and hard guards:
  - Critical terms (crash, outage, security breach, data loss, etc.)
  - High terms (fail/fails, timeout, auth/login, API 5xx, rate limits)
  - Lower-severity terms (cosmetic, typo, enhancement) reduce score.
2. Added impact signals:
  - enterprise context, sentiment, blast radius terms (all users, companywide).
3. Hybridized ONNX + heuristic prediction:
  - ONNX remains primary label source.
  - Severity-based escalation guard can raise priority when textual evidence is strong.
4. Expanded tag extraction vocabulary for better downstream triage.

D) Backend Reliability and Database Connectivity Hardening
1. Reworked MongoDB connection strategy:
  - Retry with incremental backoff.
  - Tuned pooling/timeouts/heartbeat options.
  - Connected/reconnected/disconnected/error lifecycle logging.
2. Added database readiness middleware:
  - backend/middleware/dbReady.js
3. Protected API routes with DB-readiness checks to prevent unstable failures.
4. Enhanced /api/health to return DB connectivity status and readyState.

E) Ticket Create/Update Robustness Fixes
1. Fixed ticket creation failure caused by empty customerTier payload.
2. Backend now normalizes invalid/empty customerTier to Basic.
3. Frontend create flow now avoids sending empty customerTier strings.
4. Frontend error toast now uses normalized API client error shape
  (err.message / err.data.message) instead of generic fallback only.

F) Analytics Realtime, Stability and Feature Expansion
1. Added realtime query invalidation on socket events (create/update).
2. Added periodic analytics polling + refetch on window focus.
3. Added fallback analytics synthesis from ticket list when stats API is degraded.
4. Hardened analytics rendering against malformed/null/NaN payloads.
5. Fixed runtime crash due to missing Badge import in Analytics page.
6. Added advanced analytics feature blocks:
  - SLA risk predictor (breached, at-risk, warning)
  - Auto-triage queue for unassigned tickets
  - Team workload heatmap (weighted open load)
  - Agent performance cards (resolved/open/rate/avg resolution)
  - Reopen analytics by category
  - Priority override audit summary


17) April 2026 Security Hardening and Vulnerability Remediation
---------------------------------------------------------------
This section captures the full security pass completed across backend, runtime configuration, and dependency supply chain.

A) Authentication and Secret Handling Hardening
1. Removed insecure JWT fallback key usage; JWT now requires explicit environment configuration.
2. Added startup fail-fast checks for required JWT secret and minimum entropy length.
3. Removed auth error detail leakage in token failures.
4. Prevented password reset token leakage in forgot-password responses.
5. Enforced least-privilege registration path by ignoring caller-provided role and defaulting to agent.

B) Authorization and BOLA Controls
1. Added reusable RBAC middleware:
  - backend/middleware/authorize.js
2. Applied route-level role restrictions for high-risk endpoints (users list, team management, AI health, exports).
3. Added object-level ownership/team scoping in controllers to prevent cross-user data access.
4. Hardened update endpoints against mass-assignment by allowlisting mutable fields.

C) API Abuse and Input Safety Controls
1. Added centralized rate limit policies:
  - General API limiter (default 1000 requests/hour/IP)
  - Auth limiter (default 100 requests/15 minutes/IP)
  - AI limiter (default 30 requests/5 minutes/IP)
  - Heavy export limiter (default 30 requests/15 minutes/IP)
2. Added ObjectId validation guardrails on ticket/user/team access paths.
3. Added safer search handling with escaped regex to reduce regex abuse risk.
4. Added AI payload normalization and request size sanity caps for chat/suggestion/draft routes.

D) Upload and Data Exposure Hardening
1. Tightened upload allowlist to require BOTH approved extension and MIME type.
2. Replaced local public file URL exposure with authenticated download endpoint:
  - GET /api/upload/files/:filename
3. Disabled public static upload serving by default; opt-in via environment flag only.

E) Transport and Error Surface Hardening
1. Added production HTTPS enforcement toggle (proxy-aware via x-forwarded-proto).
2. Added trust-proxy toggle for reverse-proxy deployments.
3. Tuned helmet usage and made stack trace exposure opt-in with EXPOSE_STACK_TRACES=true in development only.

F) Endpoint Governance and Visibility
1. Created backend endpoint inventory artifact:
  - backend/endpoint-inventory.txt
2. Captured mounted routers, public endpoints, and protected route groups for ongoing audit diffing.

G) Dependency Risk Remediation and Supply Chain Security
1. Resolved frontend vulnerability chain in lockfile dependencies.
2. Resolved backend high-risk dependency issues by:
  - upgrading cloudinary to secure major version
  - removing unused multer-storage-cloudinary dependency with vulnerable peer constraints
3. Verified current audit state:
  - root npm audit --audit-level=high -> 0 vulnerabilities
  - backend npm audit --audit-level=high -> 0 vulnerabilities

H) CI/CD Security Automation
1. Added GitHub Actions security workflow:
  - .github/workflows/security-ci.yml
2. Workflow includes:
  - dependency audit gates
  - Semgrep SAST
  - OWASP ZAP baseline DAST against running backend
3. Pipeline fails on high-risk findings to prevent insecure merges.
  - Executive snapshot export (PDF)

G) New Feature Foundations (Requested Product Enhancements)
Implemented MVP versions of the suggested feature set:
1. Smart SLA Risk Predictor
2. Auto-Triage Queue
3. Similar Ticket Resolver with likely fixes
4. Priority Override Audit
5. Team Workload Heatmap
6. Escalation Assistant
7. Customer Impact Score
8. Resolution Playbooks
9. Root Cause Timeline
10. Reopen Analytics
11. Agent Performance Cards
12. Executive Snapshot PDF export
13. AI Draft Reply for comments
14. Incident War Room checklist mode
15. Knowledge Base Suggestions on ticket creation

H) Schema and Domain Enhancements
Ticket schema now includes:
1. impactScore
2. priorityOverrideAudit[]
3. statusHistory[]
4. reopenCount

I) API Surface Additions
1. GET /api/tickets/executive-snapshot
  - Returns generated PDF executive report.
2. POST /api/ai/draft-reply
  - Generates customer-facing draft response with selectable tone.

J) Verified Runtime Smoke Coverage
Performed end-to-end checks for:
1. auth login
2. ticket create/update
3. ticket similar retrieval
4. ticket detail enrichment (escalation + playbook)
5. stats payload expansion
6. executive snapshot endpoint
7. AI draft-reply endpoint

K) Known Operational Notes
1. Port 5000 conflicts can occur when multiple backend sessions are open.
2. On Windows PowerShell, npm command invocation may require cmd /c wrapper.
3. After large frontend changes, hard refresh is recommended to ensure latest bundle.
- What it is:
  Real-time bidirectional event framework.
- Where used:
  backend/server.js and frontend socket client integration.
- Impact on your project:
  Enables instant UI updates on ticket create/update events, reducing stale data and manual reloads.

10. helmet, cors, compression, express-rate-limit
- What it is:
  Security and performance middleware set.
- Where used:
  backend/server.js middleware chain.
- Impact on your project:
  Improves API hardening, controls cross-origin traffic, reduces payload size, and limits abuse spikes.

Frontend Application Layer
1. React 19
- What it is:
  Component-based UI library.
- Where used:
  Entire src tree (pages/components/layout/state interactions).
- Impact on your project:
  Enables modular UI, reusable components, and fast feature iteration.

2. Vite (rolldown-vite)
- What it is:
  Fast dev/build toolchain.
- Where used:
  Root build lifecycle via vite.config.js and npm scripts.
- Impact on your project:
  Faster local development and optimized production bundles with manual chunk splitting.

3. React Router
- What it is:
  Client-side routing solution for React.
- Where used:
  App/page navigation structure.
- Impact on your project:
  Enables multi-page app behavior (Dashboard, Tickets, Team, Settings, Auth pages) in SPA form.

4. React Query (@tanstack/react-query)
- What it is:
  Server-state fetching/caching layer.
- Where used:
  API-backed pages and settings model health polling.
- Impact on your project:
  Simplifies loading/error states, caching, refetching, and optimistic UX patterns.

5. Axios
- What it is:
  HTTP client library.
- Where used:
  src/services/api.js with grouped APIs.
- Impact on your project:
  Centralized request handling, token header management, and standardized API error normalization.

6. Zustand
- What it is:
  Lightweight global state manager.
- Where used:
  src/store/useStore.js.
- Impact on your project:
  Keeps cross-page state like theme and global UI flags simple and predictable.

7. Tailwind CSS + postcss + autoprefixer
- What it is:
  Utility-first CSS framework + CSS processing pipeline.
- Where used:
  Class-based styling across src/components and src/pages.
- Impact on your project:
  Speeds UI development, keeps styles consistent, and reduces custom CSS overhead.

8. Framer Motion
- What it is:
  Animation library for React.
- Where used:
  Animated transitions in components/pages (modals/transitions/cards).
- Impact on your project:
  Improves UI feel and feedback through motion.

9. Recharts
- What it is:
  Charting library for React.
- Where used:
  Dashboard/analytics visual components.
- Impact on your project:
  Converts ticket metrics into readable charts for operational insights.

10. React Hook Form + Yup
- What it is:
  Form state/validation stack.
- Where used:
  Auth and ticket form pages.
- Impact on your project:
  Reduces boilerplate and improves input validation consistency.

AI and Data Intelligence Layer
1. onnxruntime-node
- What it is:
  ONNX inference runtime for Node.js.
- Where used:
  backend/utils/mlPredict.js.
- Impact on your project:
  Executes local ML predictions quickly without external API dependency.

2. ONNX model artifact (priority_model.onnx)
- What it is:
  Serialized ML model file.
- Where used:
  Loaded in backend inference path.
- Impact on your project:
  Provides deterministic, portable priority prediction behavior in production.

3. Python training stack (scikit-learn + skl2onnx)
- What it is:
  Offline model training and export toolchain.
- Where used:
  backend/scripts/train.py.
- Impact on your project:
  Lets you retrain and regenerate the model artifact as logic/data evolves.

4. @google/generative-ai (Gemini)
- What it is:
  Optional LLM API integration.
- Where used:
  backend/routes/ai.js for chat and suggestion logic when API key is configured.
- Impact on your project:
  Adds assistant capabilities beyond deterministic classifier behavior.


6) API and Security Design
--------------------------
Backend middleware stack:
1. helmet for secure headers.
2. cors allowlist with credentials.
3. compression for payload reduction.
4. body parser with request size limits.
5. express-rate-limit global request throttling.

Security patterns:
1. JWT route protection via auth middleware.
2. Validation layer via express-validator.
3. Centralized error handling for safe responses.
4. Auth-required AI model health endpoint.


7) AI + ML Integration Architecture
-----------------------------------
Priority prediction is integrated in ticket create flow:
1. Frontend sends ticket payload.
2. Backend ticket controller calls predictPriority(title, description, customerTier).
3. mlPredict loads ONNX model/session if available.
4. Prediction output enriches stored ticket fields.
5. If model unavailable/fails, fallback heuristic ensures continuity.

The system therefore supports both:
1. Intelligent prediction path (ONNX).
2. Operational fallback path (rule-based scoring).


8) Real-Time Events
-------------------
Socket.IO events emitted by backend:
1. ticket_created
2. ticket_updated
3. team-scoped variants using room names team_<teamId>

Usage goal:
1. Keep dashboards/lists synchronized.
2. Reduce manual refresh needs for active operators.


9) Build, Validation, and Operational Commands
----------------------------------------------
Frontend (project root):
1. npm run dev
2. npm run lint
3. npm run build
4. npm run preview

Backend (backend folder):
1. npm run dev
2. npm run start
3. npm run seed
4. npm run check:model
5. node scripts/checkModelHealth.js

Runtime smoke checks:
1. GET /api/health
2. GET /api/ai/model-health (with auth token)


10) Data and Domain Entities
---------------------------
Primary collections:
1. Users
2. Teams
3. Tickets
4. Ticket comments (embedded/subdocument behavior depending on schema design)

Ticket stores both operational and AI metadata:
1. Priority and status.
2. Tags and sentiment.
3. AI prediction confidence/reasoning.
4. SLA-oriented estimated resolution fields.


11) Production Readiness Notes
------------------------------
Strengths:
1. Clear frontend/backend separation.
2. Health checks and model health visibility.
3. Fallback logic for model failure resilience.
4. Real-time event channel for support operations.

Remaining improvement opportunities:
1. Add CI workflow for automated lint/build/smoke checks.
2. Add integration tests for auth + ticket creation + AI route.
3. Add structured logging and request correlation IDs.
4. Add backup/restore and migration strategy documentation.


12) 2026 Real-Data Migration Addendum
-------------------------------------
This section records the latest production-facing migration away from mock data.

Backend data import and schema normalization:
1. Added backend/scripts/importData.js.
2. Importer supports JSON and CSV input.
3. Added schema alias handling for common ITSM headers:
  - Incident_ID, CI_Cat, CI_Subcat, Open_Time, Handle_Time_hrs, etc.
4. Importer normalizes:
  - priority to Critical/High/Medium/Low
  - status to Open/In Progress/Resolved/Closed
  - customer tiers to supported schema enum values
5. Importer clears ticket collection, inserts transformed rows, then syncs indexes.

Ticket API upgrades:
1. Ticket list endpoint now supports comma-separated multi-filter values for status/priority.
2. Ticket create endpoint supports user priority override while preserving AI prediction metadata.
3. Ticket stats endpoint now accepts optional team scope and returns chart-ready data:
  - ticketsOverTime
  - resolutionRateTrend
  - heatmapData
  - funnelData

Similar tickets (cosine similarity):
1. Replaced tag-overlap logic with cosine similarity on title+description text vectors.
2. Similar endpoint compares target ticket with all other tickets in MongoDB.
3. Returns top 5 ranked matches with similarityScore and similarityPercent.

Frontend migration outcomes:
1. Removed active imports of src/data/mockData.js from pages.
2. Analytics page now reads fully from backend stats endpoint.
3. Team page roles/activity are derived from real team and ticket API data.
4. Create Ticket now uses API-driven similar tickets and supports AI-priority prefill + user override.
5. Ticket list bulk delete now performs real API deletes (not simulated).

Ticket detail and navigation behavior:
1. Route /tickets/:id remains the canonical detail route.
2. TicketDetail now includes a Similar Tickets section with clickable cards.
3. Clicking similar tickets updates URL and refetches detail payload for the new ID.
4. Browser back/forward navigation works through standard route history behavior.

Store and bootstrap notes:
1. Zustand store includes server-data slices (tickets/users/teams) initialized as empty arrays.
2. App shell triggers bootstrapData() to hydrate these slices from backend APIs on load.

Current known blocker for full dataset verification:
1. Repository currently does not include requested tickets.json/training_data.csv file by default.
2. Import path is ready; once dataset file is provided, importer command populates MongoDB.


12) Final Technical Summary
---------------------------
This project uses a layered architecture:
1. React/Vite/Tailwind for user-facing interface.
2. Node/Express/Mongoose for API and business logic.
3. ONNX runtime for local deterministic ML inference.
4. Optional Gemini for assistant-style AI interactions.

Practical impact:
1. Fast end-user UI and modern developer workflow.
2. Scalable API structure with secure middleware patterns.
3. AI-assisted prioritization with fallback reliability.
4. Real-time team collaboration via socket events.
---------------------------
This repository is a complete full-stack IT ticket system with:
1. Modern React frontend and API-driven architecture.
2. Node/Mongo backend with secure middleware.
3. Local ONNX ML inference for deterministic ticket prioritization.
4. Operational observability through health endpoints and scripts.
5. Extensibility for both product features and ML improvements.
